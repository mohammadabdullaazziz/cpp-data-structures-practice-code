সি++ (C++) ল্যাঙ্গুয়েজে Unary + Operator হলো এমন একটি অপারেটর যা কেবল একটি অপারেন্ডের (Operand) ওপর কাজ করে। গণিতে কোনো সংখ্যার সামনে প্লাস চিহ্ন যেভাবে ব্যবহার করি, এটি ঠিক সেভাবেই কাজ করে।

সাধারণত আমরা দুটি সংখ্যার যোগ করতে + ব্যবহার করি (যেমন: 5 + 3), একে বলা হয় Binary Operator। কিন্তু যখন এই + চিহ্নটি শুধুমাত্র একটি সংখ্যার আগে বসে, তখন একে বলা হয় Unary + Operator।

উদাহরণ: +x বা +10

Unary + Operator কোনো সংখ্যার মানের কোনো পরিবর্তন করে না।
     
যদি সংখ্যাটি পজিটিভ হয়, তবে সেটি পজিটিভই থাকে।

যদি সংখ্যাটি নেগেটিভ হয়, তবে সেটি নেগেটিভই থাকে।

সি (C) বা সি++ (C++) ল্যাঙ্গুয়েজ গাণিতিক চিহ্নের ক্ষেত্রে 
+ ও + মিলে +
+ ও - মিলে -
- ও - মিলে +

#include <iostream>
using namespace std;

int main() {
    int a = 10;
    int b = -20;

    int result1 = +a; // a এর মান ১০-ই থাকবে
    int result2 = +b; // b এর মান -২০-ই থাকবে (পরিবর্তন হবে না)

    cout << "Value of result1: " << result1 << endl;
    cout << "Value of result2: " << result2 << endl;

    return 0;
}


#include <iostream>
#include <typeinfo> // টাইপ চেক করার জন্য

using namespace std;

int main() {
    char x = 'A';

    cout << "Size of x: " << sizeof(x) << " byte" << endl;     // output: 1
    cout << "Size of +x: " << sizeof(+x) << " bytes" << endl; // output: 4
    
    // মান কিন্তু একই আছে, শুধু টাইপ বদলে গেছে
    cout << "Value of x: " << x << endl;   // দেখাবে: A
    cout << "Value of +x: " << +x << endl; // দেখাবে: 65 (যেহেতু int হয়ে গেছে)

    return 0;
}

পর্দার আড়ালে সি++ এর একটি গোপন নিয়ম কাজ করছে, যাকে বলা হয় "Integral Promotion"।

১. sizeof(x) কেন ১?
এখানে x হলো একটি char টাইপ ভেরিয়েবল। সি++ এ একটি char মেমোরিতে সাধারণত ১ বাইট (1 byte) জায়গা নেয়। তাই sizeof(x) সরাসরি ১ আউটপুট দেয়। এটা পানির মতো পরিষ্কার।

২. sizeof(+x) কেন ৪? (আসল রহস্য)
এখানেই খেলাটা বদলে যায়। সি++ এর নিয়ম অনুযায়ী, যখন কোনো ছোট ডেটা টাইপের (যেমন: char বা short) ওপর কোনো গাণিতিক অপারেটর (যেমন: +, -, * ইত্যাদি) ব্যবহার করা হয়, 
তখন কম্পিউটার হিসাব করার সুবিধার জন্য ওই মানটিকে অটোমেটিক int টাইপে রূপান্তর করে নেয়।

x এর মান হলো 'A' (যার আসকি বা ASCII ভ্যালু ৬৫)।

যখন লিখা হল +x, কম্পিউটার ভাবলো: "আচ্ছা, আমার ওপর একটা গাণিতিক অপারেটর কাজ করছে। তাহলে আমি এখন আর ছোট char থাকবো না, আমি পূর্ণাঙ্গ int হয়ে যাবো।"

তখন ওই ৬৫ মানটি একটি ইন্টিজার (Integer) হিসেবে গণ্য হয়।

যেহেতু কম্পিউটারে একটি int সাধারণত ৪ বাইট (4 bytes) জায়গা নেয়, তাই sizeof(+x) লিখলে সে আর ১ দেখায় না, বরং ইন্টিজারের সাইজ অর্থাৎ ৪ দেখায়।

char একা থাকলে সে ১ বাইট। কিন্তু তার সাথে কোনো অপারেটর (+, - ইত্যাদি) বসলেই সে "প্রমোশন" পেয়ে বড় হয়ে int হয়ে যায়। আর int মানেই হলো ৪ বাইট।
